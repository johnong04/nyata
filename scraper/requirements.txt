# scraper/requirements.txt — S3 spike deps (Python >=3.10)
scrapling>=0.4,<0.5
requests
selectolax
# Firecrawl fallback (uncomment only if scripting rung 4; else use the firecrawl-scrape skill):
# firecrawl-py
